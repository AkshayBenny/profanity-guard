{
	"name": "profanity-guard-monorepo",
	"private": true,
	"scripts": {
		"build": "pnpm --filter \"*\" run build",
		"test": "vitest run",
		"test:watch": "vitest"
	},
	"devDependencies": {
		"@types/node": "^25.6.0",
		"typescript": "^6.0.3",
		"vitest": "^4.1.5"
	}
}
